import { AppenderpipesPipe } from './appenderpipes.pipe';

describe('AppenderpipesPipe', () => {
  it('create an instance', () => {
    const pipe = new AppenderpipesPipe();
    expect(pipe).toBeTruthy();
  });
});
